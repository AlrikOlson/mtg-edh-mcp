# mtg-edh-mcp

## Quick Start

```bash
# Quality gate (check + test + lint)
# Or use /validate skill
npx tsc --noEmit && npm test && npm run lint
```

## Workflow — Plan, Execute, Verify

Every change follows this strict loop. No exceptions. The roadmap, the plan and the gates live in think-and-ship (the `roadmap_*` / `ship_*` / `think_*` / `signal_*` tools on the `magistr` MCP server); `ROADMAP.md` is a generated view.

1. **PLAN** — `roadmap_next` picks the chunk; read the codebase; `ship_set_objective` + `ship_plan` before touching source files; open a `think_record_step`.
2. **EXECUTE** — Implement with TDD (red-green-refactor). Follow `.claude/rules/conventions.md`.
3. **VERIFY** — All four steps are mandatory, in order:
   - Run quality gates: `npx tsc --noEmit && npm test && npm run lint` — recorded through `ship_check`
   - **Update the roadmap** — `roadmap_complete_chunk` for what you implemented, `roadmap_add_chunk` for anything new
   - **Commit** — one atomic commit per chunk
   - **Close** — `ship_finalize` and a closing `think_record_step`

**These rules are NON-NEGOTIABLE:**

- You MUST update the roadmap after every piece of work — no work is invisible
- You MUST commit at the end — uncommitted work is unfinished work
- You MUST pass quality gates before committing — no broken commits
- You MUST plan before executing — reading first, coding second

## Skills

| Skill              | Purpose                                                                        |
| ------------------ | ------------------------------------------------------------------------------ |
| `/next`            | Implement the next ready roadmap chunk (research -> plan -> execute -> verify) |
| `/next-loop`       | Activate autonomous iteration loop                                             |
| `/validate`        | Run quality gates                                                              |
| `/roadmap`         | Advance the roadmap one chunk at a time under the full think/ship contract     |
| `/roadmap-refresh` | Research-driven refresh of the roadmap itself (no implementation)              |
| `/signals`         | Triage stakeholder signals and promote validated ones to chunks                |
| `/craft`           | The operating doctrine behind the roadmap skills                               |
| `/init`            | Re-run project interview to update config                                      |

## Rules Reference

Detailed standards are in `.claude/rules/` (auto-loaded every session):

| File             | Scope                                                           |
| ---------------- | --------------------------------------------------------------- |
| `workflow.md`    | Universal methodology — plan-execute-verify, TDD, quality gates |
| `conventions.md` | Project-specific conventions and patterns                       |
| `tools.md`       | MCP tool inventory and usage guide (auto-generated by magistr)  |

## Configuration

Project configuration is in `.magistr/project.json`. Skills, hooks, and scripts read this file for all language-specific commands and patterns.

<!-- think-and-ship -->

# think-and-ship

The four tool families below are served **in-process by the `magistr` MCP server** (`magistr mcp-serve`) over one think-and-ship store. Do not also configure a standalone `think-and-ship serve` on this project: two processes over one store each hold their own copy of the execution and roadmap state (persistence is merge-on-save, never reload-on-read), so a write through one is invisible to the other's guards until restart. `magistr init` retires such an entry.

## Tool families

| Family        | Purpose                                                                 | Key tools                                                                                           |
| ------------- | ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| **think_***   | Reasoning trace: record steps, branch hypotheses, pin conclusions       | `think_record_step`, `think_pin_step`, `think_trace_checkpoint`                                     |
| **ship_***    | Execution trace: objectives, tasks, actions, quality gates              | `ship_set_objective`, `ship_plan`, `ship_start`, `ship_record`, `ship_check`, `ship_finalize`       |
| **roadmap_*** | The long-horizon plan-of-plans driving both                             | `roadmap_status`, `roadmap_next`, `roadmap_start_chunk`, `roadmap_complete_chunk`, `roadmap_export` |
| **signal_***  | Stakeholder signals: capture, research, surface, promote to the roadmap | `signal_capture`, `signal_pending`, `signal_research`, `signal_promote`                             |

## Cross-referencing

Link reasoning to execution:

- On `think_record_step`, pass `execution_ref: "task:<id>"` to point at a ship_* task.
- On `ship_record`, pass `think_step: <N>` to point back at the motivating think_* step.

Both halves resolve the same project identity from the working directory so
traces auto-correlate.

## Quick-start workflow

1. `ship_set_objective` — define the goal
2. `ship_plan` — break into tasks
3. `think_record_step` — record your reasoning (open)
4. `ship_start` → `ship_record` → `ship_complete` — do the work
5. `ship_check` — record test/lint results
6. `ship_finalize` — finalize the objective
7. `think_record_step` — record outcome (close)

## Verification

This is a Typescript project. Use these commands to verify changes:

- `npx tsc --noEmit`
- `npm test`
- `npm run lint`

<!-- /think-and-ship -->

<!-- archetypes -->

## Project Archetypes

**Topology:** Standalone MCP server

| Archetype   | Language   | Root | Check              | Test       | Lint           |
| ----------- | ---------- | ---- | ------------------ | ---------- | -------------- |
| mtg-edh-mcp | typescript | `.`  | `npx tsc --noEmit` | `npm test` | `npm run lint` |

<!-- /archetypes -->
